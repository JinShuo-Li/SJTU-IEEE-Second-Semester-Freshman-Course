# Digital Circuits LaTeX Project

Compile with XeLaTeX:

```bash
latexmk -xelatex Digital_Circuits.tex
```

The `assets/` directory must remain next to `Digital_Circuits.tex` because the figures use relative paths.

The final section includes the requested attribution: some figures are taken or adapted from *Digital Fundamentals* by Thomas Floyd.
